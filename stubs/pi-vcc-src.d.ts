/**
 * Type stub for deep-src imports from @sting8k/pi-vcc.
 *
 * The package ships raw .ts source that fails our strict typecheck, so a
 * tsconfig `paths` mapping points all `@sting8k/pi-vcc/src/*` imports here
 * instead of pulling that source into the program. Runtime resolution is
 * untouched (jiti loads the real files).
 */
export declare const normalize: any;
export declare const filterNoise: any;
export declare const buildSections: any;
export declare const sanitize: any;
export declare const brief: any;
